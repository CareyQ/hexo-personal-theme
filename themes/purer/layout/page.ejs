<% if (page.type === 'tags') { %>
    <%- partial('tags') %>
<% } %>

<% if (page.type === 'links') { %>
    <%- partial('links') %>
<% } %>

<% if (page.type === 'about') { %>
    <%- partial('about') %>
<% } %> 